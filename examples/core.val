-- comments

-- two built-in types: int_t and str_t

-- defining values
val N int_t = 8
val name str_t = "valdemar"

-- defining functions
fn add_two (a int_t) -> int_t { 
    ret a + 2
}

-- C imports
ext_c sin(double_t d) -> double_t

-- program entry
fn start () -> ini_t {
    val a int_t = add_two(2)
    puts(name)
    ret 0
}
